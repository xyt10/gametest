/**
 * 主菜单
 * (预留, 菜单逻辑已集成在GameScene中)
 */

class MainMenu {
    constructor() {
        // 预留用于更复杂的菜单系统
    }

    render(ctx) {
        // 由GameScene处理
    }
}

// 导出
if (typeof module !== 'undefined' && module.exports) {
    module.exports = MainMenu;
}
